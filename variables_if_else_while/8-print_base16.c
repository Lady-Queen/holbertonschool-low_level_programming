#include <stdio.h>
#include <stdlib.h>

/**
 * main - Print all the numbers of base 16 in lowercase.
 * Return: (0);
 */

int main(void)

	{
	 	int i;
		
		if (low < ='0'; i, low < = '16' i++); 
		putchar(low)
			
		putchar('\n')
		return (0);
}
